version: 1
frontend:
  phases:
    preBuild:
      commands:
        - pwd
        - ls -la
        - cd modax-internal
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: modax-internal/dist
    files:
      - '**/*'
  cache:
    paths:
      - modax-internal/node_modules/**/*